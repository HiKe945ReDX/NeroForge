version: '3.8'

services:
  api-gateway:
    build: ./backend/services/api-gateway
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URL=mongodb+srv://guidora_admin:Sris0945@sris0945.yim8u.mongodb.net/guidora_db?retryWrites=true&w=majority&appName=Sris0945
      - FRONTEND_URL=http://localhost:8080
      - ALLOWED_ORIGINS=*
    
  user-service:
    build: ./backend/services/user-service
    ports:
      - "5001:5001"
    environment:
      - MONGODB_URL=mongodb+srv://guidora_admin:Sris0945@sris0945.yim8u.mongodb.net/guidora_db?retryWrites=true&w=majority&appName=Sris0945
      - JWT_SECRET=guidora_super_secure_jwt_secret_key_2025_very_long_at_least_32_characters
    
  ai-guidance:
    build: ./backend/services/ai-guidance
    ports:
      - "5002:5002"
    environment:
      - GEMINI_API_KEY=AIzaSyDTDLfzkAzJEbqdFkR2jT9sbIITi0xsepA
      - MONGODB_URL=mongodb+srv://guidora_admin:Sris0945@sris0945.yim8u.mongodb.net/guidora_db?retryWrites=true&w=majority&appName=Sris0945
    volumes:
      - ./backend/configs/keys:/app/keys
    
  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    environment:
      - REACT_APP_API_URL=http://localhost:8080

networks:
  guidora-network:
    driver: bridge
